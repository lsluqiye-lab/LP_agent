# LP-Agent 项目宪法 (Memory & Guidelines) - v4.6.2

这是 LP-Agent (v2.0+) 的核心设计指南和持久化记忆库。所有的功能开发、策略优化和代码重构都必须遵循本文件定义的原则。

## 1. 项目愿景与核心哲学
*   **目标**：构建一个基于 LLM + ReAct 框架的、具备深度研报分析能力的自运行美股中长线趋势交易系统。
*   **核心哲学：Bear-Case-First (风险优先)**
    *   任何买入决策前，必须先穷尽所有风险因素（技术面走弱、基本面高估、消息面利空）。
    *   **没有发现明确风险 ≠ 风险很大**，而是说明技术面相对干净，可以推进到买入条件检查。
    *   看到确定性机会时必须果断行动，意识到“空仓也是有成本的”。
*   **拒绝刻舟求剑的死数字 (Dynamic Over Hardcoded)**：
    *   绝不让大模型（LLM）自己去计算滑点、容错率等微观数字，大模型只负责输出原汁原味的支撑/阻力位。
    *   所有微观执行层的数字（如挂单滑点、止损线、止盈线）必须由底层系统基于标的的实时绝对价格或波动率（如 ATR）进行动态计算，实现“千股千面”。
    *   **2026-07-24 稳定性升级 (Stability Upgrades v4.6.1)**：
        1. **超买决策冷却 (Decision Cooldown)**：在极度超买 (Sentiment > 80) 时，同一标的 30 分钟内禁止重复买入。严防大模型在工具被拦截后产生的“躁动式”连续下单。
        2. **领涨股 4.0x ATR 防线**：对于 Tier 1 高信心标的，止损宽度强制提升至 4.0x ATR。在 Stage 2 强势期，系统必须给予领袖标的足够的呼吸空间，拒绝因日内杂音而提前出局。
        3. **填充态拦截 (Filling Awareness)**：物理拦截网必须感知 `FILLING` (成交中) 状态，杜绝由于执行时差导致的“幽灵单”重复建仓。

    *   **2026-07-30 资金与频率优化 (Financing & Frequency v4.6.2)**：
        1. **现金优先原则 (Cash Priority)**：系统应优先使用可用现金，尽量避免产生新的融资负债。CIO 在做出买入决策前，必须审视当前的融资状态（Cash 余额是否为负）。若已处于融资状态，买入理由必须极度充分（A+ 级别机会）。
        2. **频率感知而非硬拦截 (Awareness over Interception)**：针对操作过频问题，系统不实施物理硬拦截，而是通过 ReAct Prompt 实时反馈当前的**“交易账单”**。如果日内微调（如 < 0.5% 的止损调整）过多，CIO 应收到明确警告，并被引导合并操作或忽略短期杂音。
        3. **合并止损决策**：鼓励 CIO 对同一板块或同一标的的多次调整进行合并处理，减少无效的“先撤后挂”动作。

    *   **2026-06-05 教训：价格胜过均线 (Price over Indicators)**。在极速杀跌行情中，均线和 RSI 是严重滞后的。系统必须优先信任**“日内跌幅/高点回撤”**等高频价格动能指标。当价格坠落速度超过阈值时，必须无视一切所谓“多头排列”的技术指标，第一时间启动避险逻辑。
    *   **2026-06-08/09 教训：警惕开盘情绪陷阱与分批建仓原则 (Opening Hysteria/Position Sizing)**。在风控评分经历剧烈跳变修复后的开盘前 30 分钟内，系统应遵循**“量价一票否决”与“先上车后补票”**原则：
        1. **量价深度确认 (Volume Veto)**：在开盘剧烈波动的横盘期，无量上涨大概率是诱多。要求系统（CIO）在突破前高时必须深度审视相对成交量，若无明显放量（如 > 1.5x），应直接行使一票否决权拒绝买入。让系统学会在指标和量价配合上更精准地扣动扳机。
        2. **初试啼声与补票**：即使量价齐升，若处于开盘跳变期，CIO 仍应下达**减半头寸（目标仓位的 30%-50%）**作为观察仓先行入场，严禁一次性满额追入。待开盘 30 分钟后价格站稳突破位，再补齐剩余头寸。
    *   **对冲期权的动态退出 (Hedge Unwinding)**：对冲期权（如 Protective Put）是防御性武器，非长线头寸。系统必须具备柔性处置能力：一旦宏观危机解除（评分回升）或该期权在暴跌中实现了超额暴利防守，CIO 必须有意识地主动平仓回收利润和本金，防止时间损耗（Theta）导致归零。绝不锁死硬性退出点，而是交由 CIO 基于量价和宏观态势动态裁决。

    *   **2026-06-16 策略审计升级 (Strategy Quality Audit)**：
        为了解决“跑平基准但波动率大”的问题，系统引入了强制性的策略质量审计：
        1. **打脸率追踪 (Whipsaw Audit)**：复盘 Agent 会自动回溯过去 3 天的卖出决策。若卖出后 72 小时内股价收回并超过卖出价，系统将记录为“Whipsaw”，并量化评估错过涨幅。
        2. **实时回补摩擦记录 (Re-entry Friction)**：CIO 在执行买入时，若发现该标的在 3 天内刚被卖出，系统会自动计算“买回成本 (Friction Cost)”。
        3. **数据导向调优**：所有审计数据存储在 `audit_logs` 中，作为参数调整（如 ATR 乘数、对冲开关）的唯一量化依据。

    *   **2026-06-18 交易频率意识与突破质量控制 (Trade Frequency Awareness & Breakout Quality)**：
        1. **交易预算意识 (Soft Limit/Trade Budget)**：在 `config.py` 中定义 `target_daily_trades`（建议 8 次）。系统不再硬性拦截交易，而是通过 ReAct 注入当前已成交次数，要求 CIO 具备“交易预算”意识。超过限额时必须在推理中提供极其充分的理由，从而在保持灵活性与节省手续费之间取得平衡。
        2. **严防“融资追高”与低质突破 (Anti-Financing Chasing)**：针对“老融资突破”等高风险诱多行为，系统建立了多重防线：
           - **突破质量分 (Breakout Score)**：技术分析师必须输出 0-100 的突破质量评分。
           - **量价一票否决升级**：对于非池内标的或融资买入，相对成交量阈值提高至 **2.0x**，且突破质量分必须 **> 80**。
           - **融资审美挑剔**：当账户处于融资状态（现金为负）时，CIO 必须以极度苛刻的眼光审视每一个突破，严禁追逐 RSI > 70 的高位突破。

    *   **2026-06-29 V-Recovery 与 Tier 分级策略优化 (V-Recovery & Tier Scaling)**：
        为了解决“震荡市左右挨打 (Whipsaw)”与“领涨股拿不住”的问题：
        1. **V-Recovery vs Bull Trap 判定边界**：明确 V-Recovery 必须满足价格收复 50%、巨量(>2.0x)且 48h 内发生的硬约束。严禁在缩量（<1.2x）、压力位遇阻（SMA20/50）或超买背离时回补，防止陷入 Bull Trap。
        2. **Tier 1 Leader 宽裕保护垫**：高信心 (Conviction: high) 标的的推荐 ATR 追踪止损倍数从 3.0x 调优至 **3.5x - 4.0x**。赋予其“翻倍潜力股”特权，允许更大的回撤呼吸空间，减少日内噪音导致的无效出局。

    *   **2026-07-06 叙事量化与深度思考树 (Narrative Engine & ToT) [v4.5.0/v4.5.1]**：
        1. **叙事量化引擎 (NME)**：引入叙事分析。CIO 必须识别全网叙事密度与情绪偏移。叙事共振时允许果断加仓，叙事偏移时必须优先避险。
        2. **深度思考树协议 (ToT)**：针对高冲突或 Tier 1 标的，强制执行 [BULL] / [BEAR] / [TAIL_RISK] 三路博弈推演。通过三方会审（Synthesis）过滤诱多陷阱，提升决策胜率。

    *   **2026-07-07 对冲动态退出与非对称原则 (Dynamic Unwinding) [v4.5.4]**：
        1. **Theta 敏感性与动态退出**：对冲期权是“高耗材”防御。当风控评分修复（如从 <70 回升至 >80）或股价企稳时，CIO 必须具备 Theta 敏感度，优先平仓 Protective Puts 回收残值，禁止死守。
        2. **非对称对冲原则**：在评分 >75 的偏多环境中，禁用昂贵的期权对冲，转而使用 `TSMPCT` (追踪止损) 等零成本工具。

    *   **2026-07-22 V4.6 主线意识与柔性防御 (Main-Line Awareness & Flexible Defense)**：
        为了解决“牛市/反弹市踏空”与“防守过头”的问题：
        1. **主线探测与偏好 (Main-Line Bias)**：引入 `GetMainLineLeadersTool`。CIO 必须实时识别当日的主力资金流向与领涨板块。主线标的允许适度超配，并给予更大的呼吸空间（4.0x ATR）。
        2. **柔性板块限制 (Soft Guardrails)**：将“板块一票否决”降级为“资金流惩罚”。允许在弱势板块中博弈 A+ 级个股机会，但头寸减半 (0.5x)，实现风险与机会的平衡。
        3. **对冲熔断 (Hedge Circuit Breaker)**：增加“暴力反弹熔断”逻辑。若大盘日内涨幅 > 1.2%，系统必须自动触发 Protective Puts 的平仓/减仓，拒绝为无效的保险支付 Theta 损耗。
        4. **叙事打脸修正 (Narrative Correction)**：要求 `NarrativeAnalyst` 进行“现实校验”。如果叙事看空但价格暴涨，必须在 15 分钟内完成叙事修正，严禁在主升浪中死守教条。

    ## 2. 核心架构：双轨制引擎 (Dual-Track Architecture V4.6.0)
    系统废弃了耗时的 10 分钟大循环，升级为双层并发架构：
    1. **高频监控层 (Watchdog)**：每 1 分钟执行一次。纯本地计算（不调用 LLM）。负责基于 ATR 的动态硬止损（跌破成本 1.5 * ATR 斩仓）与动态保护止盈（涨超成本 3.0 * ATR 锁润）。
    2. **深度决策层 (Strategic Brain)**：每天固定时间（如 10:00 和 15:30）触发。
    *   **Phase 1-2.5**: 数据收集与风控候选提取（含**主线探测**）。
    *   **Phase 3**: 专家多线程并行研报（特别强化**叙事量化**与**打脸修正**分析）。
    *   **Phase 4**: CIO 进行 ReAct 推理，执行**主线偏好**策略与**对冲熔断**。
    *   **Phase 5**: 盘后每日复盘与记忆更新。


## 3. 决策准则与约束
*   **标的池约束**：默认仅交易精选的 10-15 只标的（如 NVDA, TSM, MSFT 等）。每天开盘前可通过动态选股器更新。
*   **风控硬约束**：
    *   **坠落预警 (Crash Detection)**：新增日内动态坠落监控。若 SPY/QQQ 日内跌幅超过 1% 或从高点回撤超过 1.5%，系统将强制下调评分并进入 CAUTIOUS/LOCKDOWN 模式，无视滞后的日线均线指标。
    *   评分 < 30 (LOCKDOWN)：禁止新建仓，强制检查减仓逻辑。
    *   评分 < 50 (CAUTIOUS)：仅允许持有或减仓。
*   **买入必要条件 (松绑版)**：首选标准的 Stage 2 上升趋势 (股价 > SMA50 > SMA200)。**新增特例**：若处于 Stage 1 末期，股价强力放量突破 SMA50 且有异动资金抢筹，即便未站上 SMA200，也允许作为“底部反转/动能爆发”进行右侧建仓，防止踏空。
*   **加仓与止损逻辑 (松绑版)**：
    *   加仓不再死板要求浮盈必须大于 5%。只要底仓**不亏损 (>= 0%)** 且出现新的确认突破点，即可进行金字塔加仓。
    *   高 Beta 股票（如半导体）在震荡市极易洗盘，CIO 需谨慎使用过紧的 TSMPCT（追踪止损），应放大 ATR 倍数（如 3x）或直接依赖底层 Watchdog 的收盘跌破再进行宽容防守。

## 4. 技术规范与工程实践
*   **并发优先**：在 ReAct 循环中，应优先使用并行工具调用（如同时获取多只股票的技术分析或新闻）以节省 Token 和时间。**注意**：在进行大规模并发大模型请求（如 `Phase 3` 中多专家分析多只股票）时，必须使用 `asyncio.Semaphore` 限制并发度（推荐最大并发数=2），以防触发 429 限流并导致底层网络库死锁。
*   **大模型 JSON 提取**：大模型（特别是 Flash 级别模型）在输出 JSON 时常混杂 Markdown 标记或多余的自然语言解释。在解析 LLM 输出时，禁止使用粗暴的 `.replace("```json", "")`。必须使用正则表达式（如 `re.search(r'```json\s*(.*?)\s*```', text, re.DOTALL)` 或 `re.search(r'(\{.*\})', text, re.DOTALL)`）提取纯粹的 JSON 块。
*   **环境变量与配置更新**：当修改 `.env` 配置文件（如更换大模型名称）后，必须使用 `kill` 终止旧进程，并使用 `start.sh` 重启常驻的 `main.py` 守护进程，防止内存中驻留旧配置引发 NotFound 等连锁错误。
*   **版本控制与文件对齐 (Versioning Consistency)**：
    *   在进行任何重大功能更新或策略补丁后，必须同步检查并更新以下文件中的版本号：`CHANGELOG.md` (记录变更)、`agent/react.py` (Docstring与Prompt版本)、`start.sh` (启动Banner版本)。
    *   **2026-07-07 准则**：在更新前，务必先读取 `GEMINI.md` 或 `MEMORY.md` 确认当前基准版本，严禁跨版本或倒退版本标注。当前基准版本已正式升级为 **v4.6.1**。

    *   **2026-07-17 深度复盘与执行快照感知 (Full-Spectrum Review & Execution Snapshots) [v4.5.6]**：
        1. **执行快照感知 (Execution Snapshot)**：系统现在在每一笔买卖成交的瞬间，会自动抓取完整的技术指标（RSI, ATR, MACD, SMA, OBV等）并持久化到交易日志中。这为后续的“技术归因复盘”提供了确凿的物理证据，消除了复盘时的信息滞后。
        2. **后交易轨迹审计 (Trajectory Audit)**：新增回溯机制。复盘智能体每天会自动检查过去 7 天内所有卖出标的的后续走势，并计算“卖早了（错过收益）”或“卖得好（避开回撤）”。
        3. **指标增强可视化**：`visualizer.py` 现在能生成包含 RSI、ATR 子图的高级 K 线图，帮助 CIO 和用户直观分析每一笔交易时的“技术气象”。
*   **时区处理**：内部逻辑统一使用 `US/Eastern` (美东时间) 处理交易判断，显示输出可兼顾 `Asia/Shanghai`。
*   **异常处理**：搜索工具失败不应中断整个交易流程，记录原因后继续执行核心逻辑。
*   **LongPort OpenAPI 挂单策略与滑点控制**：
    *   长桥 OpenAPI 报 `602001` 鉴权错误时，通常是因为不支持某些追踪止损市价单（如 `TSMPCT` 和 `TSM`）。底层代码已统一平滑降级为追踪止损限价单（`TSLPPCT` 和 `TSLPAMT`）。
    *   凡是涉及限价单（LO, LIT, TSLPPCT），必须在 `tools/trading.py` 中使用 `_calculate_dynamic_slippage` 函数基于标的股价（>$500 用 0.1%，<$50 用 0.3% 等）自动注入**抢跑滑点**与**容错偏移 (limit_offset)**，严禁使用固定金额（如 0.5 美元）防止被高波动股击穿挂单。
*   **高频监控状态管理 (News Watchdog)**：基于 LLM 的高频突发新闻巡检（如每 15 分钟）必须具备**“已知事件记忆” (Stateful Memory)** 和**“冷却期” (Cooldown，如 2 小时)** 机制。由于 LLM 是无状态的，它会把持续报道的旧黑天鹅当成新事件反复报警（导致无限清仓死循环）。提示词中必须包含系统已知的异常事件列表，要求 LLM 仅对全新黑天鹅做出响应。
*   **多券商交易执行抽象 (Multi-Broker Abstraction)**：行情数据与决策分析层继续使用功能强大且稳定的长桥 (LongPort) 行情 API，而交易执行层必须抽象为统一的 `BaseTradingEngine` 适配层。系统通过在 `.env` 中读取 `TRADING_PROVIDER` (如 `longport` / `usmart`) 切换底层券商，使上层 CIO 在 ReAct 决策中调用的 `buy_stock` / `sell_stock` 保持完全一致与无感。

## 5. 待优化与演进方向 (Roadmap)
*   [ ] **对接盈立证券 (uSMART) 自动交易**：
    *   **开发方案**：集成官方支持的 `usmart-api` 库，在 `.env` 中配置 `X_Channel`、`phoneNumber`、公私钥、登录密码与交易密码。并在连接初始化时调用 `client.login()` 解锁交易通道。
    *   **订单类型平滑降级（虚拟条件单机制）**：若 uSMART 不直接支持高级服务器端追踪止损单（`TSLPPCT`）和限价触及单（`LIT`），必须利用本地 1分钟高频监控 Watchdog 机制，在本地内存/数据库中维护“虚拟高阶条件单”。当 Watchdog 监测到价格触发时，由本地在毫秒级内自动向 uSMART 报送标准的 `MARKET`（市价）或 `LO`（限价）单，从而实现与长桥完全对标的高阶交易策略。
*   [ ] **多智能体协作**：将风控决策与交易执行拆分为独立的 Agent 角色进行对抗或协作。
*   [ ] **回测闭环**：实现一个轻量化回测工具，验证 LLM 在历史数据上的决策胜率。
*   [ ] **策略库扩展**：引入更多量化指标（如威廉指标、多周期共振等）作为 LLM 的思考素材。
*   [ ] **长短期记忆优化**：优化 `TradingMemory` 的检索效率，确保历史教训能更精准地影响当前决策。

---
*本文件由 Gemini CLI 初始创建，随项目演进而不断更新。*

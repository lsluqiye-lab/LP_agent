# LP-Agent v3.5 Dual-Track 智能交易系统——全景技术架构与设计白皮书

本白皮书旨在系统性、全景式地梳理与总结 **LP-Agent v3.5 Dual-Track (双轨制 AI 量化交易系统)** 的核心设计、分布式多 Agent 协作网络、Qlib 量化中台融合，以及绝对确定性的底层风控自愈合防守机制。

经过数轮深度迭代与实战检验，系统已从传统 10 分钟单线程大循环演进为一套具备 **“Qlib 因子矿池盘前选股 + 盘中分布式多 Agent 专家协作研报 (Map) + ReAct CIO 主脑战略推理决策 (Reduce) + 毫秒级 WebSocket 高频雷达监控 + 订单绝对防守硬规则拦截与数量全自动重整自愈”** 的工业级高智能、强抗震自动化美股交易系统。

---

## 一、 系统愿景与核心设计哲学 (Philosophy)

LP-Agent 的架构设计建立在 **“风控第一，熊市思维 (Bear-Case-First)”** 与 **“决定性规则防御，让模型专注于趋势 (Dynamic Decisive Defense)”** 的双重哲学之上。

1.  **Bear-Case-First (风险优先)**：在建立多头头寸或加仓前，系统首先全面扫描技术面走弱、基本面高估、消息面黑天鹅等利空因素。在宽幅震荡与熊市市中，**守住本金与锁定利润的权重远大于追求暴利**。
2.  **绝对规则硬核防守 (Decisive Boundary Control)**：绝不让大语言模型（LLM）去拍脑袋猜测执行层面的滑点、头寸配比、可用额度或止损比例。微观执行细节（如偏移滑点、保本平衡线、自适应持仓红线、止损限幅）完全由系统底层程序基于标的价格与历史波动率（ATR）进行精确的决定性计算，智能体则专注于高屋建瓴的趋势研判，从而实现“大盘暖时让利润奔跑，大盘寒时落袋为盈”。

---

## 二、 核心架构：Dual-Track 异步双层引擎 (Dual-Track Architecture)

系统全面废弃了传统耗时且易因网络延迟锁死的阻塞式大循环，升级为**“高频监控层 (Watchdog)”** 与 **“深度决策层 (Strategic Brain)”** 双轨异步并发引擎：

```
                      ┌────────────────────────────────────────┐
                      │      WebSocket Millisecond Watchdog     │
                      │       (高频雷达监控 & 1分钟巡检)          │
                      └───────────────────┬────────────────────┘
                                          │ 跌破/突破紧急中断 (Emergency Wakeup)
                                          ▼
 ┌──────────────────────┐     ┌────────────────────────┐     ┌──────────────────────┐
 │     Qlib 量化中台     ├────►│    Strategic Brain     │◄────┤    交易日志与长期     │
 │  (因子研究/Alpha扫描)  │     │   (每日10:00 / 15:30)  │     │   记忆 (114条规则库)   │
 └──────────────────────┘     └───────────┬────────────┘     └──────────────────────┘
                                          │
                                          ▼ (Phase 1-2.5)
                        【数据收集、风控线性打分、候选股初筛】
                                          │
                                          ▼ (Phase 3 - Map)
                        【多专家并行研报 (asyncio.Semaphore=2)】
                          ├─ Technical Analyst (多周期量价/均线共振)
                          ├─ Fundamental Analyst (估值催化剂/财报规避)
                          ├─ Sentiment Analyst (突发新闻/极度情绪)
                          └─ Quant Analyst (Qlib 因子对齐/大盘RS比)
                                          │
                                          ▼ (Phase 4 - Reduce)
                        【ReAct CIO 主脑战略决策推理 & 冲突锁秒撤挂单】
                                          │
                                          ▼ (后置自愈钩子)
                        【auto_align_trailing_stops()】数量自愈对齐
                                          │
                                          ▼ (Phase 5 - Review)
                        【盘后自动复盘更新记忆 & 发送飞书图文卡片】
```

---

## 三、 Qlib 量化中台深度融合 (Qlib Integration)

系统引入微软开源 AI 驱动量化平台 **Qlib**，构建了强大的“离线大数据因子池研究 + 盘前 Alpha 因子扫描 + 盘中实时因子对齐”的三位一体量化中台：

1.  **Phase 0: 盘前阿尔法扫描 (Alpha Scanner)**：
    *   **离线因子库 (Alpha158)**：利用 Qlib 离线计算美股全量（`region="us"`）历史 K 线，生成高维因子库（包含量价变动率、波动率偏离、跨周期相对强弱等 158 个量化特征）。
    *   **机器学习模型预测**：盘前由 `alpha_scanner.py` 加载基于 Qlib 训练的 GRU/LightGBM 阿尔法选股模型，对当前标的池进行阿尔法排序（Alpha Ranking）。
    *   **财报雷区规避**：自动交叉校验未来 5 个交易日内是否有财报窗口（Earnings Report），强行滤除高爆雷标的，动态输出今日黄金自选名单（`daily_watchlist.json`）。
2.  **盘中实时因子对齐 (Online Feature Alignment)**：
    *   在分布式研报阶段，`QuantAnalyst` (量化分析专家) 将长桥 OpenAPI 拉取的实时 250 日 K 线数据与 Qlib 进行因子级对齐，计算包含：
        *   **均线多周期共振**：SMA50 与 SMA200 的阶梯量化。
        *   **相对强度 (Relative Strength)**：计算个股 20 日表现相对于标普 500 指数 (SPY) 的绝对跑赢乘数 $RS\_ratio$。
        *   **量能催化度 (Volume Ratio)**：当天实时成交量与历史 50 日均量的倍数关系（vol_ratio），作为主力暗中吸筹/放量突破的量化准则。

---

## 四、 多智能体“Map-Reduce”并行协作网络 (Multi-Agent Network)

系统的决策链路被优雅地抽象为多智能体集群的 Map-Reduce 网络，实现了高度的模块化边界与极强的专业研判深度。

### 1. Map 阶段：分布式多专家多线程研报
由协调器 `ExpertOrchestrator` 调度，为防海量 API 请求触发大模型 429 频限和底层死锁，特设 **`asyncio.Semaphore(2)` 限制并发度**。4 个独立的专业 Agent 并行对活跃候选股展开全景解剖：
*   **Technical Analyst (技术面专家)**：解析均线趋势、多周期成交量价格形态、以及支撑位与阻力位（拒绝拍脑袋数字，提供纯粹的技术边界）；
*   **Fundamental Analyst (基本面评估专家)**：分析财务乘数（PE、PB）、行业壁垒、近期财报健康度以及未来的业绩增长催化剂；
*   **Sentiment Analyst (舆情监控专家)**：抓取社交媒体讨论热度与新闻，分析大盘/个股当前处于极度恐慌还是贪婪，防范情绪踩踏；
*   **Quant Analyst (Qlib量化专家)**：输出 Qlib 因子对齐后的高阶技术评分和大盘 RS 比率。

### 2. Reduce 阶段：ReAct CIO 主脑决策推理
主决策智能体 `ReActAgent` (首席信息官 CIO) 接收所有专家汇总输出的异构非结构化研报（Briefings JSON）、PortfolioManager 下达的自适应投资红线（Directives）及实时仓位。
*   **Thought-Action-Observation 闭环**：通过严格的 ReAct 推理链，自动校验行动是否越界。
*   **自我纠偏 (Self-Correction)**：如果技术面显示极度超买，CIO 会在 Thought 中自我纠错，拒绝 BUY 动作，从而实现极富纪律性的动作下达。

---

## 五、 绝对决定性的多重风控与自愈合防守算法 (Decisive Defense)

大模型擅长宏观分析，但在严谨的价格控制和资产保护上存在天生缺陷。LP-Agent 3.5 在底层执行组件上，通过 6 大确定性逻辑筑起了无法被击穿的规则钢铁长城：

### 1. 线性插值动态风控评分系统 (Linear Risk-Regime)
系统拒绝对持仓上限和止损实行一刀切的死板红线。风控评分自适应大盘温度，在 $S \in [0, 100]$ 的范围内进行**连续线性插值**，平滑输出：
$$\text{Max Single Stock Exposure} = 5\% + \frac{S - 30}{100 - 30} \times (15\% - 5\%)$$
$$\text{ATR Trailing Stop Multiplier} = 1.5 + \frac{S - 30}{100 - 30} \times (3.5 - 1.5)$$
当风控环境从 favorable 渐变到 cautious 时，持仓额度随之平滑收紧，推荐的 ATR 追踪止损随之拉紧（例如从 3.16x 收缩到 1.8x ATR），确保在微观阶跃点不产生订单频繁被砍的误操作，实现“极度丝滑”的防守缩防。

### 2. 追踪比例硬性区间防雷锁 (Sanity Check)
为彻底杜绝大模型在计算 ATR 追踪止损比例时挂出过宽（回吞全部利润和本金）或过窄（因日内小噪声扫损出局）的坏参数，底层 `SellStockTool` 直接在执行前强行限制 $trailing\_percent \in [3.0\%, 12.0\%]$。如果超出上限，**自动强制截断并重置为 12.0%**，并记录 Sanity Warning。

### 3. 滑点抢跑与限价偏移算法 (`_calculate_dynamic_slippage`)
挂非市价单（LO / LIT / 条件单）时，系统自动调用滑点算法。例如高波动高价股（如 NVDA, ASML）大单，系统自动在价格中注入万分之十（0.1%）的抢跑溢价（Buy 单高报，Sell 单低报），并配以动态的 `limit_offset` 偏移。**确保在市场高暴动交易时段，高级挂单能被瞬间吃掉成交，杜绝被行情“洗盘”踏空或击穿挂单的风险。**

### 4. 冲突挂单秒撤机制 (Cancel-Before-Modify)
为规避券商端对同一标的重复提交止损单而重复冻结、扣减可用资金以及订单冲突：
当下达 LO/LIT/TSMPCT 等挂单指令时，系统底层自动检索今天所有该标的同向的 Pending 条件单（如 `PENDING_CONDITIONAL` 状态），**秒撤旧单，短暂休眠 0.5s 等待券商额度释放，随后挂设新单**，彻底闭环了挂单冲突死锁。

### 5. 新闻 Watchdog Stateful 状态记忆与 2 小时避险冷却 (Stateful Cooldown)
突发核弹级利空新闻（如地缘战争）发生时，无状态的 LLM 每 15 分钟都会将“同一条旧闻”误判为“全新黑天鹅”，引发系统每 15 分钟强行平仓的死循环。
*   **状态记忆** (`known_critical_events`)：本地持久化记录已告知大模型的黑天鹅事件，命令其跳过旧闻；
*   **警报冷却** (`WATCHDOG_COOLDOWN_SECONDS = 7200`)：对同一标的的相同中断事件强制开启 2 小时防守冷却锁，杜绝因地缘事件报道导致多重爆破式清仓。

### 6. 订单数量全自动重整自愈机制 (Auto-Alignment)
解决由于加仓、部分减仓导致“已持有的股份未被原本的追踪止损全额覆盖”的硬伤漏洞（防漏挂）：
*   在每次 CIO 决策（Phase 4）结束后，后置钩子会执行 **`auto_align_trailing_stops()`**；
*   系统自动检索当前最新实际持仓（如 FCX 持仓 188 股）和现存追踪止损挂单数量（如仅挂了 170 股）；
*   一旦识别到股数不吻合，**自动执行旧单秒撤，并以最新 188 股持仓、维持原本的止损比例重新挂单**。
*   这使系统在遭受盘中因拆单、多次加仓导致的股数不一致时，具备**极强的架构级自愈能力**。

---

## 六、 高鲁棒性与异常平滑设计 (Robustness)

1.  **极度超买禁止 LO 接飞刀**：
    风控系统强制阻断：当市场情绪 Sentiment $> 80$ 或 RSI $> 75$ 时，交易工具绝对拦截买入的左侧 `LO` 限价单（防止高位接下坠的飞刀），强制 CIO 必须改用右侧突破确认单（`LIT` 突破限价单），极大地锁死了逆势做多的致命诱惑。
2.  **高级订单平滑降级**：
    针对长桥 OpenAPI 对部分 `TSMPCT` 追踪比例市价单的 602001 报错，交易工具层自动降级转译为追踪限价单 `TSLPPCT`（配合滑点计算），使用户端及智能体端无阻碍挂单。
3.  **时区标准统一**：
    内部时序统一转换至 **`US/Eastern` (美东时间)** 进行状态判断，展示与飞书推送卡片动态映射到 `Asia/Shanghai`，保证极佳的交互健壮度。

---

*本白皮书作为 LP-Agent v3.5 Dual-Track 系统的终极技术架构白皮书，已正式发布并同步推送至 GitHub 远端。*
